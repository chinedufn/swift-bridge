[package]
name = "swift-integration-tests"
version = "0.1.0"
edition = "2021"
publish = []

build = "build.rs"

[lib]
crate-type = ["staticlib"]

[build-dependencies]
swift-bridge-build = {path = "../swift-bridge-build"}

[dependencies]
lazy_static = "1.4.0"
swift-bridge = {path = "../../"}
